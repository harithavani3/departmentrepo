server:
  port: 8084 
spring:
  application:
    name: department_service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
